//IF STATEMENT

let age = 16;
let state = 'CA';

if (state == 'CA') {
    if (age >= 16) {
        console.log('You can drive');
    }
}

// OR

if (state == 'CA' && age == 16) {
    console.log('You can drive');
}